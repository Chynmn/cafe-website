<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.tukorea.starbucks.mapper.UserMapper">

    <insert id="register" useGeneratedKeys="true" keyProperty="num" parameterType="UserVO">
        insert into user (id, passwd, userName, email, point) values
            (#{id}, #{passwd}, #{userName}, #{email}, #{point})
    </insert>

    <select id="login" resultType="UserVO">
        select id, passwd, userName, point from user where id = #{id} and passwd = #{passwd}
    </select>

</mapper>